import { BookText, CheckCircle2, Menu, XCircle } from 'lucide-react';
export function TopBar({ onMenuClick, onEvidenceClick, hasCitations, ready }: { onMenuClick: () => void; onEvidenceClick: () => void; hasCitations: boolean; ready: boolean | null }) {
  const available = ready === true;
  return <header className="bg-surface border-b border-outline-variant flex justify-between items-center px-md py-sm h-14"><div className="flex items-center gap-2"><button onClick={onMenuClick} className="lg:hidden" aria-label="Menu"><Menu /></button><strong className="text-primary">Trợ lý Luật Lao động</strong></div><div className="flex items-center gap-3"><span className={available ? 'text-secondary text-sm' : 'text-error text-sm'}>{available ? <CheckCircle2 className="inline w-4" /> : <XCircle className="inline w-4" />} {ready === null ? 'Đang kiểm tra' : available ? 'Hệ thống sẵn sàng' : 'Chưa sẵn sàng'}</span><button onClick={onEvidenceClick} aria-label="Căn cứ pháp lý"><BookText className={hasCitations ? 'text-secondary' : ''} /></button></div></header>;
}
