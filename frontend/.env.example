# Public browser configuration only. Never put credentials or private URLs here.
# Docker Compose leaves this unset so the React app calls same-origin FastAPI via
# the Nginx /api, /health, and /ready proxies.
# For `npm run dev`, point Vite at a locally running FastAPI server:
VITE_API_BASE_URL=http://localhost:8000
